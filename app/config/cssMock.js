// Jest has no CSS loader; Uniwind applies global.css via the
// Metro/Babel transform at build time, not in the Jest environment, so a plain
// import can be mapped to nothing here.
module.exports = {};
