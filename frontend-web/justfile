# Frontend Web Task Runner
# Run `just --list` to see all available commands
# Run from root: `just frontend-web/<recipe>` or from frontend-web/: `just <recipe>`

# Show available recipes
default:
    @just --list

# ============================================================================
# Development
# ============================================================================

# Install dependencies
install:
    npm ci

# Start development server
dev:
    npm run dev

# ============================================================================
# Building
# ============================================================================

# Build for production
build:
    npm run build:prod

# Build for staging (loads .env.staging via --mode staging)
build-staging:
    npm run build:staging

# ============================================================================
# Quality Checks
# ============================================================================

# Run all checks: Biome lint/format + Astro type and template checks
check:
    npm run check

# Run Astro's project validation without rerunning Biome
astro-check:
    npx astro check

# Auto-format code with Biome
format:
    npm run format

# Lint with Biome (read-only)
lint:
    npm run lint

# ============================================================================
# Testing
# ============================================================================

# Run unit tests
test:
    npm run test

# Run tests with coverage report (for CI)
test-cov:
    npm run test:coverage

# Run end-to-end tests (requires a prior `just build`)
test-e2e:
    npm run test:e2e

# Open Playwright UI for interactive e2e test development
test-e2e-ui:
    npm run test:e2e:ui

# Run all tests (unit + e2e)
test-all:
    npm run test:all

# ============================================================================
# Security
# ============================================================================

# Scan dependencies for known vulnerabilities
audit:
    npm audit --audit-level=high --omit=dev
    @echo "✓ Dependency audit complete"

# Clean caches and build artifacts
clean:
    rm -rf .astro coverage dist playwright-report test-results
    @echo "✓ Cleaned caches and build artifacts"
