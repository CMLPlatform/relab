import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  screen: {
    flex: 1,
    gap: 16,
    padding: 32,
    maxWidth: 800,
    alignSelf: 'center',
  },
});

export default styles;
