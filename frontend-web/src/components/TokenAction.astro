---
interface Props {
  title: string;
  apiUrl: string;
  loadingMessage: string;
  successMessage: string;
  noTokenMessage: string;
  defaultErrorMessage: string;
}

const { title, apiUrl, loadingMessage, successMessage, noTokenMessage, defaultErrorMessage } =
  Astro.props;
---

<section
  class="card stack-md"
  data-token-action
  data-api-url={apiUrl}
  data-success-message={successMessage}
  data-no-token-message={noTokenMessage}
  data-default-error-message={defaultErrorMessage}
>
  <h1>{title}</h1>
  <div id="status" class="status status-loading" data-token-status role="status" aria-live="polite">
    <div class="spinner" aria-hidden="true"></div>
    <p>{loadingMessage}</p>
  </div>
</section>
