[project]
  dependencies    = []
  description     = "Documentation for the Reverse Engineering Lab"
  name            = "docs"
  requires-python = ">=3.13"
  version         = "0.1.0"

[dependency-groups]
  build = ["zensical>=0.0.27"]
  serve = ["fastapi>=0.115.0", "uvicorn>=0.42.0"]
