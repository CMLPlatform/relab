# E2E test build environment for the Expo web app.
# Used by `npx expo export -p web` when running full-stack E2E tests.
# The backend is served by compose.e2e.yml on localhost:8000.
EXPO_PUBLIC_API_URL='http://localhost:8000'
EXPO_PUBLIC_WEBSITE_URL='http://localhost:8010'
